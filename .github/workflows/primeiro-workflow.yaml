name: Devops workflow

on:
  push:
    branches: 
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Mostrar mensagem de boas-vindas
      run: echo "Bem-vindo ao meu primeiro workflow de DevOps!"
      